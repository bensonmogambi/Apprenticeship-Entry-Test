import requests

def fetch_user():
    url = "https://jsonplaceholder.typicode.com/users/1"
    
    try:
        print("Loading user data...")   # Loading state
        response = requests.get(url, timeout=5)

        # Check for successful response
        if response.status_code == 200:
            user = response.json()
            print("User Data Fetched Successfully")
            print("Name:", user["name"])
            print("Email:", user["email"])
        else:
            print(" Error: Unable to fetch data. Status code:", response.status_code)

    except requests.exceptions.RequestException as e:
        print(" Error: Could not connect to API -", e)

# Example
fetch_user()





##Explanation

#I used the requests library to fetch data from the API.
#A loading state is shown with "Loading user data...".
#If successful, the user’s name and email are displayed.
#If something goes wrong, an error message is shown.
